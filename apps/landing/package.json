{
  "name": "@superpool/landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3001",
    "prebuild": "pnpm --filter @superpool/types run build && pnpm --filter @superpool/ui run build && pnpm --filter @superpool/assets run build",
    "build": "next build",
    "start": "next start --port 3001",
    "lint": "eslint . --config ../../eslint.config.mjs",
    "type-check": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@superpool/assets": "workspace:*",
    "@superpool/design": "workspace:*",
    "@superpool/types": "workspace:*",
    "@superpool/ui": "workspace:*",
    "next": "16.1.1",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
