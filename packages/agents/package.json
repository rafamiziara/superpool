{
  "name": "agents",
  "version": "0.1.0",
  "description": "SuperPool Mastra agents — the assessment service",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "mastra start",
    "test": "vitest run",
    "eval": "tsx src/mastra/evals/run.ts",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --config ../../eslint.config.mjs",
    "format": "prettier --write . --ignore-path ../../.prettierignore",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mastra/auth": "^1.0.0",
    "@mastra/core": "^1.40.0",
    "@mastra/libsql": "^1.12.0",
    "@mastra/loggers": "^1.1.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "dotenv": "^17.3.1",
    "mastra": "^1.11.0",
    "tsx": "^4.23.12",
    "typescript": "~6.0.3",
    "vitest": "^5.0.0"
  }
}
