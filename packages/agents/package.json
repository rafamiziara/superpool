{
  "name": "agents",
  "version": "0.1.0",
  "description": "SuperPool Mastra agents — the assessment service",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "mastra start",
    "test": "jest",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --config ../../eslint.config.mjs",
    "format": "prettier --write . --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "@mastra/auth": "^1.0.0",
    "@mastra/core": "^1.40.0",
    "@mastra/libsql": "^1.12.0",
    "@mastra/loggers": "^1.1.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.1",
    "jest": "^30.4.2",
    "mastra": "^1.11.0",
    "ts-jest": "^29.4.6",
    "typescript": "~6.0.3"
  }
}
